apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: shopping-list
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: marijacvetanovska/shoppinglist-frontend:latest
          ports:
            - containerPort: 80