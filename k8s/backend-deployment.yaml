apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: shopping-list
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: marijacvetanovska/shoppinglist-backend:latest
          ports:
            - containerPort: 3000